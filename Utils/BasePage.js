class BasePage {
    constructor(page) {
    this.page = page; // Store the Playwright page object
    }

    // You can add common methods or properties for all pages here
}

module.exports = BasePage;
